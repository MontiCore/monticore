package de.monticore.umlcd4a.parser;

classdiagram Example1 {

  // Parsing constructor and method
  class A {
    public A();
    public void foo();
  }

  // Parsing constructor and method with Parameter
  class B {
    public B(int i);
    public void foo(int i);
  }
  
  // Parsing interface with attribute and method
  interface I {
    int att = 0;
    private int foo();
  }
}