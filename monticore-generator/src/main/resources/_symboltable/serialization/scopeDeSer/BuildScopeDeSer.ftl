<#-- (c) https://github.com/MontiCore/monticore -->
${tc.signature("scopeDeSerName")}
  ${scopeDeSerName} scopeDeSer = new ${scopeDeSerName}();
  scopeDeSer.setSymbolFileExtension(symbolFileExtension);
  return scopeDeSer;