/* (c) https://github.com/MontiCore/monticore */
description = 'Experiments: scannerless'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/de/monticore/Scannerless.mc4"
  outputDir = file outDir
  def uptoDate = incCheck("de/monticore/Scannerless.mc4")
  outputs.upToDateWhen { uptoDate }
}
