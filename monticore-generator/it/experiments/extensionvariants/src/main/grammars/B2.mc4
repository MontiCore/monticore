/* (c) https://github.com/MontiCore/monticore */
grammar B2 extends A {

  // Harmless overwriting existing NT's with other keywords

  // Orig:  M = Decimal; 
  M = Integer;

  // Orig:  N = "one" M;
  N = "ein" M "ende";

  
  New = Integer;
}
