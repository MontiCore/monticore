/* (c) https://github.com/MontiCore/monticore */
description = 'Experiments: symbols'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/Automaton.mc4"
  outputDir = file outDir
  handcodedPath file("$projectDir/src/main/java")
  def uptoDate = incCheck(outputDir.get().asFile.toString() + "/automaton/IncGenGradleCheck.txt")
  outputs.upToDateWhen { uptoDate }
}
