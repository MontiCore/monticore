description = 'Experiments: symbols'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/Automaton.mc4"
  outputDir = file outDir
  handcodedPath "$projectDir/src/main/java"
  def uptoDate = incCheck(outputDir.toString() + "/automaton/IncGenGradleCheck.txt")
  outputs.upToDateWhen { uptoDate }
}