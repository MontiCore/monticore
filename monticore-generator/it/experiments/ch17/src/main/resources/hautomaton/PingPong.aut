/* (c) https://github.com/MontiCore/monticore */
automaton PingPong {
  autstate NoGame <<initial>>;
  autstate InGame <<final>> {
    autstate Ping ;
    autstate Pong ;

    Ping - returnBall > Pong;
    Pong - returnBall > Ping;
  }

  NoGame - startGame > Ping;

  InGame - stopGame > NoGame;
}
