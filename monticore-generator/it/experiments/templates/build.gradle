/* (c) https://github.com/MontiCore/monticore */
dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    implementation project(":automaton_experiments")
}
outDir = "$buildDir/statepattern"

sourceSets{
    test.java.srcDirs += [outDir]
}
