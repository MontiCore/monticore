description = 'Experiments: patternAccess'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/Example.mc4"
  outputDir = file outDir
  handcodedPath "$projectDir/src/main/java"
  def uptoDate = incCheck(outputDir.toString() + "/example/IncGenGradleCheck.txt")
  outputs.upToDateWhen { uptoDate }
}