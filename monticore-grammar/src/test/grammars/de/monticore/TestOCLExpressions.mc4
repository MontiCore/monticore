package de.monticore;

grammar TestOCLExpressions extends de.monticore.OCLExpressions, de.monticore.CommonExpressions {
	PrimaryExpression implements Expression<20000> = 
	  Name;
	  OCLCollectionVarDeclaration implements Expression = varName:Name "in" collection:Name;
	  
	  OCLNestedContainer implements Expression = varName:Name "in List <" imput:Name ">";
	  
	  OCLDeclaration implements Expression = (public:"public" | private:"private")? type:Name varName:Name;
}