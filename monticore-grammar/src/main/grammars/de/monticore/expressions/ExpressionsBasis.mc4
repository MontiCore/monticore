/* (c) https://github.com/MontiCore/monticore */

package de.monticore.expressions;

component grammar ExpressionsBasis extends de.monticore.MCBasics, de.monticore.MCLiteralsBasis {
	
  interface Expression;

  symbol EMethod = Name;

  symbol EVariable = Name;

  symbol EType = Name;

  NameExpression implements Expression<350>
    = Name;

  LiteralExpression implements Expression<340>
    = Literal;

  QualifiedNameExpression implements Expression <290> =
  	Expression "." Name;
}
