/* (c) https://github.com/MontiCore/monticore */
description = 'Experiments: hwDeSers'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/ColoredGraph.mc4"
  outputDir = file outDir
  def uptoDate = incCheck("ColoredGraph.mc4")
  outputs.upToDateWhen { uptoDate }
}
