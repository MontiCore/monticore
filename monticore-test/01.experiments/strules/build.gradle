/* (c) https://github.com/MontiCore/monticore */
description = 'Experiments: symbolrule and scoperule'

task generate (type: MCTask) {
  grammar = file "$projectDir/$grammarDir/STRules.mc4"
  outputDir = file outDir
  def uptoDate = incCheck("STRules.mc4")
  outputs.upToDateWhen { uptoDate }
}
