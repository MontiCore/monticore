/* (c) https://github.com/MontiCore/monticore */
description = 'MontiTrans Test: DSTLGen'

// Make the trafo sourceset available to tests
sourceSets {
  test {
    compileClasspath += sourceSets.trafo.output
    runtimeClasspath += sourceSets.trafo.output
  }
}
configurations {
  testImplementation.extendsFrom(trafoImplementation)
}

tasks.named('testClasses') {
  dependsOn tasks.named('trafoClasses')
}

// Expose the trafo sources

configurations {
  trafoOut {
    extendsFrom(trafoImplementation)
  }
}
tasks.register("trafoJar", Jar) {
  archiveClassifier = "trafo"
  from sourceSets.trafo.output
}
artifacts {
  trafoOut trafoJar
}